// Sample command to demonstrate command registration pattern
// This command will be removed in future phases when real commands are implemented

export function testCommand(value: string) {
  console.log(`Test command executed with value: ${value}`);
}
